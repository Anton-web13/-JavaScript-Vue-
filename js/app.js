let str = 'Hello world';
console.dir(str.length);
console.dir(str.slice(1, -1));

console.dir(String('hello').slice(1, -1));

const strObj = new String('hello');
console.dir(strObj);

console.dir(Number());
console.dir(Number);
